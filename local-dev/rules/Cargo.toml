[workspace]
members = [
    "hello-world",
    "echo-json",
    "counter",
    "cron-heartbeat",
    "webhook-router",
    "rate-limiter",
    "request-logger",
    "todo-api",
    "http-proxy",
    "error-handling",
    "postgres-reader",
]
resolver = "2"

[profile.release]
lto = true
strip = "symbols"
